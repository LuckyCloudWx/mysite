/**
 * 记录所有事件及其处理函数
 * {
 * scroll事件，传递参数为：标题doms；
 * mainScroll事件，传递参数为：主区域dom；
 * setScroll事件，传递参数为：希望的scrollTop值；
 * }
 */

// const eventContainer = {};

import Vue from "vue";

export default new Vue({});